import HomeContainer from '../container/HomeContainer';

const routes = [
    {
        prefix: '',
        path: '/',
        component: HomeContainer,
        exact: false,
        title: 'Home'
    }
]

export default routes;